import {
  project as projectCommand,
  revisionLines as projectRevisionLines,
} from "../contexts/project/adapters/ProjectCommand.ts";

export const project = projectCommand;
export const revisionLines = projectRevisionLines;
